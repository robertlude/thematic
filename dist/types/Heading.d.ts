import React, { ReactNode } from 'react';
export default function Heading({ children, className, level, }: {
    children: ReactNode;
    className?: string;
    level?: number;
}): React.JSX.Element;
